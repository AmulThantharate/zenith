pipeline {
  agent any 
  tools {
    nodejs "node25"
  }
  environment {
    SONAR_HOME = tool "sonar"

    IMAGE_NAME          = 'claw4321/zenith-backend'
    DOCKER_REGISTRY_URL = 'https://index.docker.io/v1/'
    TRIVY_SEVERITY      = 'HIGH,CRITICAL'
    TRIVY_EXIT_CODE     = '0'
    SYFT_FORMAT         = 'spdx-json'
  }
  stages {
    stage("Checkout") {
      steps{
        git branch: "ci/jenkins", url: "https://github.com/AmulThantharate/zenith.git"
      }
    }

    stage("Imaeg Tag") {
      steps{
        script{
          env.IMAGE_TAG = sh(script: 'git log -1 --format="%h"', returnStdout: true).trim()
        }
      }
    }

    stage('Install Dependencies & Test') {
            parallel {
                stage('Install Dependencies') {
                    steps {
                        dir('backend') {
                            sh '''
                                echo "=== Installing Dependencies ==="
                                npm ci --prefer-offline --no-audit
                            '''
                        }
                    }
                }
                stage('Lint') {
                    steps {
                        dir('backend') {
                            sh '''
                                echo "=== Running Linter ==="
                                npm run lint || true
                            '''
                        }
                    }
                }
          }
    }

    stage('SonarQube Analysis') {
      steps {
          dir('backend') {
              withSonarQubeEnv('sonar') {
                  sh """
                      echo "=== Running SonarQube Analysis ==="
                      ${SONAR_HOME}/bin/sonar-scanner \
                                -Dsonar.projectName='BackendService' \
                                -Dsonar.projectKey='BackendService' \
                                -Dsonar.sources=. \
                                -Dsonar.tests=. \
                                -Dsonar.test.inclusions=**/*.test.js,**/*.spec.js \
                                -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info \
                                -Dsonar.coverage.exclusions=**/*.test.js,**/*.spec.js,**/node_modules/**
                        """
                    }
                }
            }
    }

    stage("Quality Gate"){
        steps{
            timeout(time: 5, unit:"MINUTES"){
                waitForQualityGate abortPipeline: true
            }
        }
    }

    stage("Trivy File System Scan") {
            steps {
                sh "mkdir -p reports && trivy fs --format table -o reports/trivy-fs-report.html ."
            }
            post {
                always {
                    publishHTML([
                        allowMissing: false,
                        alwaysLinkToLastBuild: true,
                        keepAll: true,
                        reportDir: 'reports/',
                        reportFiles: 'trivy-fs-report.html',
                        reportName: 'Trivy FS Scan'
                    ])
                }
            }
    }

    stage("Docker Build and Tag") {
        steps {
            dir('backend') {
                sh "docker build -t zenith-backend:${IMAGE_TAG} ."
                sh "docker tag zenith-backend:${IMAGE_TAG} ${IMAGE_NAME}:${IMAGE_TAG}"
            }
        }
    }

    stage('Security Scans') {
            parallel {
                stage('Trivy Vulnerability Scan') {
                    steps {
                        sh """
                            echo "=== Running Trivy Scan ==="
                            trivy image \
                                --severity ${env.TRIVY_SEVERITY} \
                                --exit-code ${env.TRIVY_EXIT_CODE} \
                                --format table \
                                --scanners vuln \
                                --timeout 10m \
                                ${env.IMAGE_NAME}:${env.IMAGE_TAG}
                        """
                    }
                    post {
                        always {
                            sh """
                                echo "=== Generating Trivy SBOM ==="
                                trivy image \
                                    --format cyclonedx \
                                    --output trivy-backend-sbom.json \
                                    ${env.IMAGE_NAME}:${env.IMAGE_TAG}
                            """
                            archiveArtifacts artifacts: 'trivy-backend-sbom.json', allowEmptyArchive: true
                        }
                    }
                }
                stage('Syft SBOM Generation') {
                    steps {
                        sh """
                            echo "=== Generating SBOM with Syft ==="
                            syft ${env.IMAGE_NAME}:${env.IMAGE_TAG} \
                                -o ${env.SYFT_FORMAT} > syft-backend-sbom.json
                        """
                        archiveArtifacts artifacts: 'syft-backend-sbom.json', allowEmptyArchive: true
                    }
                }
            }
    }

    stage("Docker Push") {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'DOCKER_CRED',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh '''
                    echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
                    docker push $IMAGE_NAME:$IMAGE_TAG
                    '''
                }
        }
        post {
                success {
                    sh """
                        echo "=== Image Digest ==="
                        docker inspect --format='{{index .RepoDigests 0}}' ${env.IMAGE_NAME}:${env.IMAGE_TAG} \
                            || echo "Digest not available locally"
                    """
                }
        }
    }
  }
}
